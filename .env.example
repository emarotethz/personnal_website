# Copy to .env for local dev; set the real values in the Netlify UI for production.
# openssl rand -base64 32
HMAC_SECRET=
# openssl rand -hex 16
ADMIN_TOKEN=
# Local only: lets X-Debug-IP override the client IP so the per-IP cap is testable.
ALLOW_DEBUG_IP=false
